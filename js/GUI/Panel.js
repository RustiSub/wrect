(function(){
    "use strict";
    /**
     * @augments BaseGuiElement
     * @type {void|*}
     */
    window.Panel = window.BaseGuiCollection.extend({
      init: function(options) {
          this._super(options);
      }
    });
})();
